# -- coding: utf-8 --
import config

class Properties():
    host = config.host
    user = config.user
    password = config.passwd
    database = config.db
    table = config.table
    charset = config.charset
    restlogger = config.restlogger
    pastedirectory = config.pastedirectory